---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={cn(
  "rounded-lg border border-paper-border bg-paper-card text-paper-text",
  className
)}>
  <slot />
</div>
